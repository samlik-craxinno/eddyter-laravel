# Copy this file to `.env` in this folder (`cp resources/vite/.env.example resources/vite/.env`).
# Vite reads only this directory (see vite.config.js `envDir`), so edits to the project
# root `.env` do not restart the Vite dev server.
VITE_APP_NAME=Laravel
